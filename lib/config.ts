export const config = {
  // App settings
  app: {
    name: "eT's AI Builder",
    description: "AI-powered UI builder with multi-model support using Puter's keyless APIs",
    version: "1.0.0",
  },

  // Puter configuration - No API keys needed!
  puter: {
    scriptUrl: "https://js.puter.com/v2/",
    enabled: true,
  },
}

export default config
